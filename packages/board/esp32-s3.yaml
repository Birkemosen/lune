esphome:
  platformio_options:
    platform: platformio/espressif32@6.12.0
    platform_packages:
      - platformio/toolchain-xtensa-esp-elf@14.2.0+20251107
    board_build.flash_mode: qio
    board_build.psram_type: qio
    board_build.extra_flags:
      - -DBOARD_HAS_PSRAM

esp32:
  board: esp32-s3-devkitc-1
  flash_size: 4MB
  framework:
    type: esp-idf
    version: recommended
    sdkconfig_options:
      CONFIG_ESP32S3_DEFAULT_CPU_FREQ_240: "y"
      CONFIG_ESPTOOLPY_FLASHSIZE_4MB: "y"
      CONFIG_SPIRAM: "y"
      CONFIG_SPIRAM_MODE_QUAD: "y"
      # Prefer PSRAM for large networking/crypto/BLE allocations to keep
      # internal heap available for the HTTP(S) client tasks (Helios/Asgard/
      # Asgard) and the dashboard web server.
      CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP: "y"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      # --- Internal-heap relief -------------------------------------------------
      # BLE(Bluedroid)+WiFi+dashboard leave only ~16 KB free internal heap at
      # idle, low enough that
      # even the plain-HTTP Asgard push fails to allocate a socket
      # (ESP_ERR_HTTP_CONNECT). ESPHome's psram: component runs SPIRAM in
      # CAPS_ALLOC mode, so plain malloc()/new always lands in INTERNAL RAM —
      # only explicit MALLOC_CAP_SPIRAM allocations use PSRAM. We therefore can't
      # rely on a global malloc threshold; instead push the big *explicit* PSRAM
      # consumers there and trim BLE's standing footprint.
      #
      # Allocate the Bluedroid host environment dynamically (→ PSRAM, per
      # BT_ALLOCATION_FROM_SPIRAM_FIRST) instead of as static internal BSS.
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"
      # BTHome is passive scan-only — we never open a GATT connection. Trim the
      # controller's BLE activity instances (scan/adv/connect; 828 B each, S3
      # uses the C3-family BT_CTRL_BLE_MAX_ACT, default 6) and the Bluedroid
      # host ACL connection count (default 4) to what scan + a margin needs.
      CONFIG_BT_CTRL_BLE_MAX_ACT: "4"
      CONFIG_BT_ACL_CONNECTIONS: "1"
      # mDNS is disabled; Helios HTTP uses a 3s socket timeout so 15s is
      # sufficient headroom for DNS + connect + transfer on weak WiFi.
      CONFIG_ESP_TASK_WDT_TIMEOUT_S: "15"
      CONFIG_LWIP_MAX_LISTENING_TCP: "5"
      # Widen the BSD socket pool. The default of 10 is too tight: the dashboard
      # httpd can hold up to 7 connections and lwIP reserves 3, which leaves zero
      # for the native API *and* outbound esp_http_client clients (Asgard /
      # Asgard) — accept() then floods with ENFILE (errno 23) and Open-Meteo
      # fetches fail. 16 (the Kconfig max) covers httpd(7) + control(3) + API(1)
      # + client(2) + headroom. Costs ~2-3 KB internal RAM, freed by dropping the
      # stock web_server.
      CONFIG_LWIP_MAX_SOCKETS: "16"
      CONFIG_HTTPD_MAX_URI_LEN: "128"
      # loopTask (ESPHome main loop) runs every component loop() plus the BLE
      # advertise lambda. The 3584 B default left only ~672 B headroom, risking
      # stack overflow during BLE bursts; 8 KB gives comfortable margin. RAM use
      # is ~16%, so the extra few KB is free.
      CONFIG_ESP_MAIN_TASK_STACK_SIZE: "8192"
      # FreeRTOS runtime stats — lets the dashboard report per-core CPU load and
      # per-task CPU% / stack headroom (uxTaskGetSystemState) so a task that
      # saturates a core or starves the loop can be pinpointed. The stats-
      # formatting helper functions are deliberately NOT enabled (we compute the
      # percentages ourselves) to keep the flash cost to a few KB.
      CONFIG_FREERTOS_USE_TRACE_FACILITY: "y"
      CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS: "y"

# Enable the ESPHome `psram:` component so the debug platform can expose a
# Free PSRAM sensor. The IDF sdkconfig above already enables SPIRAM at the
# kernel level; this block is purely for ESPHome runtime visibility.
psram:
  mode: quad
  speed: 80MHz
