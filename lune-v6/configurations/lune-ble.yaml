# =============================================================================
# Lune V6 \u2014 BLE Configuration (BTHome temperature sensors)
# =============================================================================
# Build:   make build
# Deploy:  make deploy
#
# Includes BLE passive scanning for BTHome temperature sensors (Shelly BLU
# H&T, etc.). HA integration via the ESPHome native API.
# Helios + custom dashboard are present.
# =============================================================================

substitutions:
  device_name: lune-v6
  friendly_name: "Lune V6 Controller"

packages:
  base: !include ../lune.yaml
  ble:  !include ../packages/hardware/ble.yaml
  led:  !include ../packages/hardware/led.yaml
