# 8 physical 1-Wire probe slots plus logical role sensors derived from runtime mapping.
# Probe slots are discovered by index once, then rebound to persisted ROMs on boot.
sensor:
  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 0
    name: "Probe 1 Temperature"
    id: probe_temp_1
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 1
    name: "Probe 2 Temperature"
    id: probe_temp_2
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 2
    name: "Probe 3 Temperature"
    id: probe_temp_3
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 3
    name: "Probe 4 Temperature"
    id: probe_temp_4
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 4
    name: "Probe 5 Temperature"
    id: probe_temp_5
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 5
    name: "Probe 6 Temperature"
    id: probe_temp_6
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 6
    name: "Probe 7 Temperature"
    id: probe_temp_7
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: dallas_temp
    one_wire_id: onewire_bus
    index: 7
    name: "Probe 8 Temperature"
    id: probe_temp_8
    entity_category: diagnostic
    update_interval: ${onewire_update_interval}
    resolution: ${onewire_resolution_bits}
    filters:
      - sliding_window_moving_average:
          window_size: 3
          send_every: 1

  - platform: template
    name: "Zone 1 Temperature"
    id: temp_zone_1
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(0);

  - platform: template
    name: "Zone 2 Temperature"
    id: temp_zone_2
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(1);

  - platform: template
    name: "Zone 3 Temperature"
    id: temp_zone_3
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(2);

  - platform: template
    name: "Zone 4 Temperature"
    id: temp_zone_4
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(3);

  - platform: template
    name: "Zone 5 Temperature"
    id: temp_zone_5
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(4);

  - platform: template
    name: "Zone 6 Temperature"
    id: temp_zone_6
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_zone_temperature(5);

  - platform: template
    name: "Manifold Flow Temperature"
    id: temp_manifold_flow
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_manifold_flow_temperature();

  - platform: template
    name: "Manifold Return Temperature"
    id: temp_manifold_return
    unit_of_measurement: "°C"
    accuracy_decimals: 1
    update_interval: 2s
    lambda: return id(hv6_zones).get_manifold_return_temperature();

  - platform: template
    name: "Zone 1 Valve Position"
    id: zone_1_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(0);

  - platform: template
    name: "Zone 2 Valve Position"
    id: zone_2_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(1);

  - platform: template
    name: "Zone 3 Valve Position"
    id: zone_3_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(2);

  - platform: template
    name: "Zone 4 Valve Position"
    id: zone_4_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(3);

  - platform: template
    name: "Zone 5 Valve Position"
    id: zone_5_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(4);

  - platform: template
    name: "Zone 6 Valve Position"
    id: zone_6_valve_position
    unit_of_measurement: "%"
    accuracy_decimals: 0
    update_interval: 5s
    lambda: return id(hv6_zones).get_valve_position(5);

text_sensor:
  - platform: template
    name: "Zone 1 State"
    id: zone_1_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(0);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));

  - platform: template
    name: "Zone 2 State"
    id: zone_2_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(1);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));

  - platform: template
    name: "Zone 3 State"
    id: zone_3_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(2);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));

  - platform: template
    name: "Zone 4 State"
    id: zone_4_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(3);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));

  - platform: template
    name: "Zone 5 State"
    id: zone_5_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(4);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));

  - platform: template
    name: "Zone 6 State"
    id: zone_6_state
    update_interval: 5s
    lambda: |-
      auto snap = id(hv6_zones).get_zone_snapshot(5);
      return std::string(hv6::zone_display_state_to_string(snap.display_state));
