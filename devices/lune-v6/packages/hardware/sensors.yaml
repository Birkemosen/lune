# System sensors — motor current (published by valve controller), WiFi signal, device uptime
sensor:
  - platform: template
    name: "Motor Current"
    id: motor_current_adc
    unit_of_measurement: "mA"
    accuracy_decimals: 1
    entity_category: diagnostic
    update_interval: never

  - platform: template
    name: "Motor 1 Ripple Count"
    id: motor_1_ripple_count
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 500ms
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_live_ripple_count());

  - platform: template
    name: "Motor 1 Learned Open Ripples"
    id: motor_1_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(0).learned_open_ripples);

  - platform: template
    name: "Motor 1 Learned Close Ripples"
    id: motor_1_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(0).learned_close_ripples);

  - platform: template
    name: "Motor 2 Learned Open Ripples"
    id: motor_2_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(1).learned_open_ripples);

  - platform: template
    name: "Motor 2 Learned Close Ripples"
    id: motor_2_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(1).learned_close_ripples);

  - platform: template
    name: "Motor 3 Learned Open Ripples"
    id: motor_3_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(2).learned_open_ripples);

  - platform: template
    name: "Motor 3 Learned Close Ripples"
    id: motor_3_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(2).learned_close_ripples);

  - platform: template
    name: "Motor 4 Learned Open Ripples"
    id: motor_4_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(3).learned_open_ripples);

  - platform: template
    name: "Motor 4 Learned Close Ripples"
    id: motor_4_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(3).learned_close_ripples);

  - platform: template
    name: "Motor 5 Learned Open Ripples"
    id: motor_5_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(4).learned_open_ripples);

  - platform: template
    name: "Motor 5 Learned Close Ripples"
    id: motor_5_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(4).learned_close_ripples);

  - platform: template
    name: "Motor 6 Learned Open Ripples"
    id: motor_6_learned_open_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(5).learned_open_ripples);

  - platform: template
    name: "Motor 6 Learned Close Ripples"
    id: motor_6_learned_close_ripples
    unit_of_measurement: "ripples"
    accuracy_decimals: 0
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return static_cast<float>(id(hv6_valves).get_telemetry(5).learned_close_ripples);

  - platform: template
    name: "Motor 1 Learned Open Factor"
    id: motor_1_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(0).learned_open_current_factor;

  - platform: template
    name: "Motor 1 Learned Close Factor"
    id: motor_1_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(0).learned_close_current_factor;

  - platform: template
    name: "Motor 2 Learned Open Factor"
    id: motor_2_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(1).learned_open_current_factor;

  - platform: template
    name: "Motor 2 Learned Close Factor"
    id: motor_2_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(1).learned_close_current_factor;

  - platform: template
    name: "Motor 3 Learned Open Factor"
    id: motor_3_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(2).learned_open_current_factor;

  - platform: template
    name: "Motor 3 Learned Close Factor"
    id: motor_3_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(2).learned_close_current_factor;

  - platform: template
    name: "Motor 4 Learned Open Factor"
    id: motor_4_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(3).learned_open_current_factor;

  - platform: template
    name: "Motor 4 Learned Close Factor"
    id: motor_4_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(3).learned_close_current_factor;

  - platform: template
    name: "Motor 5 Learned Open Factor"
    id: motor_5_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(4).learned_open_current_factor;

  - platform: template
    name: "Motor 5 Learned Close Factor"
    id: motor_5_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(4).learned_close_current_factor;

  - platform: template
    name: "Motor 6 Learned Open Factor"
    id: motor_6_learned_open_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(5).learned_open_current_factor;

  - platform: template
    name: "Motor 6 Learned Close Factor"
    id: motor_6_learned_close_factor
    unit_of_measurement: "x"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_valves).get_telemetry(5).learned_close_current_factor;

  - platform: template
    name: "Zone 1 Preheat Advance"
    id: zone_1_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(0);

  - platform: template
    name: "Zone 2 Preheat Advance"
    id: zone_2_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(1);

  - platform: template
    name: "Zone 3 Preheat Advance"
    id: zone_3_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(2);

  - platform: template
    name: "Zone 4 Preheat Advance"
    id: zone_4_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(3);

  - platform: template
    name: "Zone 5 Preheat Advance"
    id: zone_5_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(4);

  - platform: template
    name: "Zone 6 Preheat Advance"
    id: zone_6_preheat_advance_c
    unit_of_measurement: "°C"
    accuracy_decimals: 2
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return id(hv6_zones).get_zone_preheat_advance(5);

  - platform: wifi_signal
    name: "WiFi Signal"
    id: wifi_signal_dbm
    update_interval: 30s

  - platform: uptime
    name: "Uptime"
    update_interval: 60s

text_sensor:
  - platform: template
    name: "Motor 1 Last Fault"
    id: motor_1_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(0).last_fault_code)};

  - platform: template
    name: "Motor 2 Last Fault"
    id: motor_2_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(1).last_fault_code)};

  - platform: template
    name: "Motor 3 Last Fault"
    id: motor_3_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(2).last_fault_code)};

  - platform: template
    name: "Motor 4 Last Fault"
    id: motor_4_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(3).last_fault_code)};

  - platform: template
    name: "Motor 5 Last Fault"
    id: motor_5_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(4).last_fault_code)};

  - platform: template
    name: "Motor 6 Last Fault"
    id: motor_6_last_fault
    entity_category: diagnostic
    update_interval: 10s
    lambda: |-
      return {hv6::fault_code_to_string(id(hv6_valves).get_telemetry(5).last_fault_code)};
